# My App

This repo contains both frontend and backend.

## Structure
- `/frontend` → React/Next.js app (deployed to Netlify)
- `/backend` → Node/Express API (deployed to Render)

## Deployment
- Netlify builds from `/frontend`
- Render builds from `/backend`

## Local Development
1. Run backend:
   ```bash
   cd backend && npm install && npm start
